Steps To run:
1)go to project repository and hit command- "npm install --save"
2)After that hit- "node app.js"
3) server will start on http://localhost:3000, so just hit it.
4) click on "get results"  button and you will see on screen.


Dependencies-
1)Nodejs should be installed on system